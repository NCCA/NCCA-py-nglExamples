# Agent session — 2026-07-17

## Goal

"Merge it all to Version1.0": integrate every outstanding agent branch.

## What happened

The HDRI/PBR fix branches (`agent/hdri-cube-orientation`,
`agent/hdri-ibl-baker`) were already merged the day before. Merged the
remaining three: `agent/project-briefs` (docs only),
`agent/smoketest-cli` (`smoketest_all.py`), `agent/qml-panel-resize`
(resizable DraggablePanel in both GUIDemos overlay demos). All merges
clean, no conflicts.

## Verification

- `uv run pytest` on Version1.0 → 322 passed
- `uv run ruff check smoketest_all.py` → clean (repo-wide ruff noise is
  pre-existing, none of it from the merged files)
- `GUIDemos/QMLOverlayApp` and `GUIDemos/QMLWebGPUOverlay` `--smoketest`
  → OK
